/// <reference types="svelte" />
/// <reference types="vite/client" />

/** The commit the engine was built from, which `vite.config.ts` puts in at build time. */
declare const __ENGINE_COMMIT__: string;
